import { createSlice } from '@reduxjs/toolkit';

const emailTemplateSlice = createSlice({
  name: 'emailTemplate',
  initialState: { emailTemplate: null },
  reducers: {},
});

// export const {} = authSlice.actions;

export default emailTemplateSlice;
